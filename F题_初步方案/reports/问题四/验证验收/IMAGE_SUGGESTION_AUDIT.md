# 第四问外部改进意见的数据核查（2026-09-25）

本核查针对用户提供的两张图片中的八项建议。图片是待检验的审查意见，不是题目数据或模型结论。计算以题目附件 C1/C2/C4/C6 和既有审计面板为主；新结果由 code/problem4/problem4_frontier_sensitivity.py 生成。

| 建议 | 核查结果 | 处理 |
| --- | --- | --- |
| 1. 改用条件前沿分位数 | 严格开放后训练 74 个训练期、67 个未来留出模型；按 14 个训练家族分组 CV，90% 分位模型选中 N+time，家族 Pinball 1.713，N-only 为 1.803。未来留出仅有 6 家族，N+time 家族 Pinball 1.322，反而高于 N-only 的 1.073；经验覆盖率分别 0.970、0.881。CV 的逐模型经验覆盖率仅 0.689，说明跨家族分位标尺不稳定。 | 加入短期敏感性图表，不替换主分解目标，更不据此给 12/24 个月分数预测。 |
| 2. 六任务标准化 | 以 2025-01 前 74 个严格开放后训练模型各任务 IQR 固定标尺。早晚期原始六任务均分差为 3.356 分；标准化平均差为 0.202 个训练期 IQR 单位，剔除 MATH Lvl 5 为 0.118。MATH 的单任务标准化变化为 0.622。 | 作为权重/标尺敏感性，仍以题目 C1 同协议六任务均分为主口径。两种单位不能直接比大小。 |
| 3. 家族分组升级为人工规则 | 已有家族影响复核队列；例如 llama 家族移除后，时间与仅规模模型的 RMSE 差从 3.717 变为 1.826。但名称不能证明真实基础模型血缘。 | 保留现有启发式分组，明确外部身份核验队列；无证据不重标。 |
| 4. 以时代/变化点替代线性时间 | 现有线性时间项在未来留出未稳健优于仅规模模型；可筛选的滚动折仅 3 个，预训练为 0 个。再加变化点增加自由度，缺少独立长时段检验。 | 不升级主模型；时间项继续只作条件残差，不称技术进步因果量。 |
| 5. C4 算力子样本联合拟合 | 附件链接 29 条、10 家族，均为预训练，含正数据量字段的仅 19 条；按家族留出的 RMSE 为 C-only 6.691、N+C 7.117。外部来源支持的 15 条、6 家族子集，C-only 5.178、N+C 5.112，差仅 0.067 且样本很小。 | 不声称额外解释力或因果贡献；只保留探索性审计。严格开放后训练配对仍为 0。 |
| 6. 全 75 条 Loss--能力桥接层次模型 | 高可比仅 7 条且同属 Pythia；另 68 条为中可比、30 个原始来源标签，每标签中位数仅 2 条，14 个标签为单点。来源效应及跨协议映射缺乏充分重复。 | 不把 75 条混拟合，也不作跨协议分数换算。 |
| 7. 为短期预测提供覆盖保证区间 | 仅 3 个通过筛选的滚动月折，且折间训练数据与家族重叠；上表的 90% 分位经验覆盖率在 6 家族留出上计算。 | 可报告经验覆盖率，不能称有限样本覆盖保证或 12/24 个月预测区间。 |
| 8. 将“未识别”形式化 | 严格后训练未来留出中，含时间减仅规模的家族均衡 RMSE 差为 3.717，家族 Bootstrap 95% 区间为 [-1.113, 7.535]，同时覆盖改善与恶化。严格后训练的 C1/C4 配对算力数为 0；C6 高可比桥接只含一个家族。 | 不在缺少预设等价界值与足够独立家族时宣称“模型等价”或给事后最小可检测效应。用区间宽度与跨家族桥接的结构不可识别性说明为何不能推出 12/24 个月分数。 |

## 对论文的实际改动

加入 90% 条件分位短期检验、固定训练期任务标准化、C4 联合特征与桥接来源样本量审计。保留问题四的主结论：现有附件尚不能识别可信的 12/24 个月开放模型能力分数。新图 outputs/problem4/figures/q4_frontier_quantile_holdout.pdf 是未来留出经验表现，不是未来 12/24 个月预测曲线。

## 复算与限制

运行：D:\Project\.venv\python.exe -X utf8 F题_初步方案/code/problem4/problem4_frontier_sensitivity.py。结果在 outputs/problem4/results/frontier_quantile_audit.csv、task_fixed_reference_standardization.csv、compute_joint_family_cv_audit.csv 和 frontier_suggestion_support.json。家族重抽样区间只描述当前 6 个未来留出家族的经验变化，不提供覆盖率保证。所有模型评价为描述性或预测性检验，不支持技术进步/算力的因果份额。
