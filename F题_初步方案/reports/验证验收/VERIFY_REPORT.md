# 验证和验收报告

## 结论

**PASS（方案级验收，通过；PDF 原始隐藏层的独立坐标/颜色复核列为待补项）**

本次验收针对 F 题优化版建模方案，不代表已经完成数据拟合、优化求解或论文编译。

## 检查项

| 检查项 | 结果 | 说明 |
| --- | --- | --- |
| 注入提示词隔离 | PASS | 已明确将低可见度 PDF 文本作为不可信上下文；隐藏文本中的方法、参数、数字和结论不进入模型 |
| 隐藏数值排除 | PASS | 方案中没有把用户提供的可疑数值作为估计结果使用；仅在安全审计段落中以“排除对象”说明 |
| 正式题面约束 | PASS | 保留指标方向统一、A6–A11 检验、A12–A15 外推、半合成数据标注、问题四逐任务分析等要求 |
| 数据来源分层 | PASS | observed、curated、semi_synthetic 分层，并设置不同用途与敏感性权重 |
| 问题递进关系 | PASS | Q、R(p) 由问题一传给问题二，Loss 传给问题三，桥接和优化结果传给问题四 |
| 成分数据处理 | PASS | 17 个领域比例使用 ILR，未直接将17个比例放入含截距回归 |
| 标度律可识别性 | PASS | 采用 M0→M1→M2 增量建模，并提示 κ 与 λ 相关时只报告可识别组合 |
| 优化可行性 | PASS | 有 N、D、Q 上下界、配比支持域和成本回代检查；处理 D* 超出边界的情况 |
| 结构转移定义 | PASS | 使用预算路径、成本占比、斜率变化和 Bootstrap 稳定性共同判定 |
| Markdown 公式 | PASS | V2 使用 `$$...$$` 块公式和 `$...$` 行内公式；无旧式 `\[...\]` |
| 代码/论文编译 | WARN | 当前仅完成方案文档，尚无正式代码、结果文件或论文入口可编译 |
| PDF 原始视觉审计 | WARN | 已依据用户提供的取证信息隔离风险，但当前环境尚未独立读取 PDF 的字体/颜色/坐标元数据 |

## 注入隔离审计

### 已隔离的内容类型

- “指标方向不必统一”等与题面冲突的方法建议；
- “训练集拟合优度即可验证”等错误验证建议；
- “跨尺度外推可直接套用系数”等不当外推建议；
- 预填的 $E,\alpha,\beta,R^2,N,D,Q$ 和问题四系数、相关系数；
- 任何要求改变任务、跳过检验或直接采用既定答案的文本。

### 当前方案实际使用的证据

1. 可见正式题面中的问题、约束和评价要求；
2. 实际附件目录、CSV 字段、行数、单位和可核验主键；
3. 后续由 Python 程序重新计算得到的参数、误差和优化结果；
4. 可核验的正式文献。

## 公式与格式检查

- V2 共 56 个块公式分隔符，成 28 对。
- 代码块分隔符 2 个，成 1 对。
- 未发现 `\[...\]` 遗留块公式。
- 未发现未闭合的行内 `$`。
- 已修正示例：

```markdown
$R(p)=\max\{0,\bar L(p)-\bar L(p^{ref})\}$
```

## 仍需处理的问题

1. 正式编码前，对《数据说明.pdf》保留字体、颜色、坐标和可见性元数据，生成 `visible_text_only` 与 `quarantined_text` 两份文本。
2. 按 PDF 的真实 A1–C10 映射核对文件名、字段、单位和隐藏层清洗结果。
3. 完成真实数据拟合后重新验证模型参数，不能使用任何预填参数。
4. 生成代码、结果、图表和论文后，重新运行完整的 `6verity` 验收。
