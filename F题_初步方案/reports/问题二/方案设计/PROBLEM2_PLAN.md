# 问题二建模思路与实施方案

## 1. 任务目标与递进接口

问题二需要建立同时含参数规模 $N$、训练数据量 $D$、数据质量 $Q$ 和领域配比 $p$ 的广义标度律，并分析边际效用、弹性、领域替代或互补，以及“质量提升 0.1 等价于参数增加多少”的条件。

本问采用模块化证据融合，不把附件 A、B 的独立实验直接拼成一张表：

1. B1 识别经典 $N-D$ 标度律；
2. B6 识别质量对数据项的修正；
3. 问题一的 A4–A15 响应面识别配比项；
4. 三部分在同一退化约束下组合，分别验证并共同传播不确定性。

问题一向本问传入可观测质量域的 $Q$ 及区间、17 域稳健参考配比 $p^{ref}$、配比相对参考配比的局部 Loss 响应 $Δ_p(p)$，以及配比模型支持域。本问不重复读取 A1–A16 原始文件。

A8–A11 文件名中的 1M、60M、1B 表示**模型参数规模 $N$**。因此问题一已有的配比效应衰减必须写成 $w_p(N)$，不能写成数据量 $D$ 的函数。

## 2. 附件 B 的实际数据审计

| 编号 | 数据 | 实际规模 | 本方案用途 | 证据等级 |
| --- | --- | ---: | --- | --- |
| B1 | Pythia 训练日志 | 8 条参数规模轨迹，每条 147 点，共 1176 点 | 经典标度律主拟合 | 真实观测 |
| B2 | Cerebras 训练日志 | 7 条轨迹，共 1029 点 | 模型族外压力测试 | 半合成 |
| B3 | Pythia 插值轨迹 | 8×500 点 | 数值插值和残差形态检查 | 插值，不是独立样本 |
| B4 | 跨族收敛点 | 57 点、12 个模型族 | 跨族趋势验证 | 真实，但 Loss 口径可能有族差异 |
| B5 | 文献标度律数据 | 44 点、9 个模型族 | 文献外部验证 | 真实整理数据 |
| B6 | N-D-Q 网格 | 360 点 | 质量效应主识别 | 半合成 |
| B7 | 扩展 N-D-Q 网格 | 450 点 | Q 网格敏感性 | 半合成 |
| B8 | 大规模 N-D-Q 网格 | 1704 点 | 数据一致性审计和远域压力测试 | 半合成、含外推 |
| B9 | 100B 以上模型元数据 | 132 点 | 外推范围定义 | 真实元数据 |
| B10 | 大模型 Loss | 128 点 | 远域风险边界 | 估算，不作为拟合真值 |
| B11/B12 | 家族和检查点索引 | 18/1386 行 | 分组、主键和来源核验 | 辅助 |

### 2.1 必须处理的审计发现

1. B1 的 1176 行是 8 条高度相关的训练轨迹。拟合可使用全部点，验证和 Bootstrap 必须以参数规模轨迹为组，不能随机拆行。
2. B3 由 B1 插值得到，只能检查数值连续性，不能作为独立验证集。本方案选择 B2 完成题目要求的 B2/B3 验证，并明确 B2 为半合成压力测试。
3. B4/B5 来自不同模型族和资料来源。即使都叫交叉熵 Loss，也可能存在语料、分词器和验证集偏移；外部验证同时报告原始误差、去均值误差、Spearman 和一参数截距校准后误差。
4. B6/B7 中相邻 Q 水平约 75% 呈“Q 增大、Loss 降低”，其余变化受半合成噪声影响。
5. B8 与题面“Q 越高越好”的定义明显冲突：只有约 15.6% 的相邻 Q 变化表现为 Loss 不升；在与 B6 重合的 160 个 $(N,D,Q)$ 点上，B8 与 B6 的 Loss 差中位数约为 -1.265。因此 B8 不进入主拟合，也不擅自翻转 Q；只作为一致性失败案例和压力测试。题目允许 B6、B7、B8 三者选一，主质量模型使用 B6，B7 作敏感性。

## 3. 可检验假设

### H1：经典规模效应

在同一 Pythia 数据和验证口径下，Loss 随 $N,D$ 增大而下降，并存在不可约下限 $E$。

### H2：质量修正作用于有效数据项

控制 $N,D$ 后，较高 Q 主要降低数据不足项，不直接改变参数不足项。该假设由 B6 的完整 $(N,D,Q)$ 网格检验。

### H3：领域配比是独立的局部 Loss 修正

附件 B 没有配比变量，配比效应只能由问题一独立识别。统一假设为：在参考配比附近，配比引起的相对 Loss 变化可叠加到 B 类规模基线上，且幅度随参数规模 $N$ 变化。

### H4：退化一致性

当 $Q=1$、$p=p^{ref}$ 时，广义模型必须退化为经典标度律。不满足该条件的候选模型不作为正文主模型。

## 4. 三级模型

### 4.1 M0：经典标度律

$$
L_0(N,D)=E+A N^{-α}+B D^{-β}.
$$

约束为 $E≥0$，$A,B,α,β>0$。采用对数参数化的加权非线性最小二乘或稳健最小二乘。B1 每条轨迹总权重相同，防止一条轨迹因检查点多而支配估计。参数区间使用按轨迹重抽样的 Bootstrap，并用 Profile Likelihood 检查 $E,α,β$ 的弱识别。

### 4.2 M1：质量修正标度律

$$
L_1(N,D,Q)=E+A N^{-α}+B D^{-β} exp[γ_Q(1-Q)], \quad γ_Q≥0.
$$

等价有效数据量为

$$
D_{eff}=D exp[-γ_Q(1-Q)/β].
$$

该形式在 $Q=1$ 时严格退化到 M0，且 Q 接近 0 时不会像 $DQ^κ$ 一样产生数值奇点。B6 首先直接识别组合参数 $γ_Q=κβ$，避免强行分别解释不可识别的 $κ,β$。只有 Profile Likelihood 支持时，才报告 $κ=γ_Q/β$。

拟合顺序是：先由 B1 得到 M0 参数分布，再在 B6 中估计 $γ_Q$ 和 B6 来源截距；最后允许 M0 参数在 Bootstrap 分布范围内小幅联合更新。B6 不与 B1 无权混合。

#### 问题一 Q 与 B6 Q 的尺度衔接

问题一 Q 是由 22 个质量信号构造的相对指数，B6 的 Q 是半合成实验控制变量。两者都在 $[0,1]$ 内，但没有成对观测证明数值一一对应。主分析采用透明的同尺度假设 $Q_B=Q_A$，并做保持完美质量锚点不变的仿射敏感性：

$$
Q_B=1-b_Q(1-Q_A), \quad b_Q∈\{0.5,1.0,1.5\}.
$$

超出 $[0,1]$ 时截断。只有当质量边际效用和参数等价量在三种标定下方向一致，才写入主结论。问题一 Q 的 Bootstrap 区间同时传入标定。

由于 11 个配比域没有独立 Q，不能用 Loss-informed 代理拼成精确的 17 域质量。问题二中的 Q 表示固定配比下的整体数据处理质量，配比差异由独立的 $Δ_p(p)$ 表示，以减少质量与配比重复计量。

### 4.3 M2：质量与配比的广义标度律

$$
L(N,D,Q,p)=E+A N^{-α}+B D^{-β} exp[γ_Q(1-Q)]+w_p(N)Δ_p(p).
$$

其中

$$
Δ_p(p)=R(p)-R(p^{ref}), \quad Δ_p(p^{ref})=0.
$$

$R(p)$ 来自问题一支持域内的 17 域响应面，$w_p(N)$ 由 A 类 1M、60M、1B 实测表估计。候选形式为

$$
w_p(N)=(N/N_0)^{-ρ_p}, \quad ρ_p≥0.
$$

同时与分段常数和向零收缩比较。若三个实测尺度不能稳定识别 $ρ_p$，正文只报告三个尺度的经验值及区间，不给伪精确指数。

M2 满足 $Q=1,p=p^{ref}$ 时退化为 M0。由于 B 类没有 p，M2 不报告把 A、B 直接合并后的虚假总 RMSE；规模、质量和配比模块分别验证，再用 Monte Carlo 合成预测区间。

## 5. 参数估计和模型选择

### 5.1 M0 拟合

1. 检查 $C≈6ND$ 的单位和记录一致性，但不把 C 与 N、D 同时作为回归自变量。
2. 对 B1 拟合 M0，Huber 损失作为异常检查。
3. 做 8 次留一参数规模轨迹验证，报告 RMSE、MAE、Spearman 和偏差。
4. 检查残差随 $D,N$ 和训练阶段的结构；若早期训练系统偏离幂律，增加最小训练阶段敏感性，不为追求拟合优度随意删除早期点。

### 5.2 M1 拟合

比较三个质量函数：

$$
g_1(Q)=exp[γ_Q(1-Q)], \quad g_2(Q)=Q^{-γ_Q}, \quad g_3(Q)=1+γ_Q(1-Q).
$$

主模型优先选择无奇点的 $g_1$。以 B6 的完整 $(N,D)$ 单元为分组做交叉验证：每次留出完整 $(N,D)$ 组合，而不是随机留出若干 Q 行。B7 只检查 Q 网格扩展后参数和方向是否稳定。

### 5.3 M2 配比接口

1. 仅在问题一训练支持域内计算 $Δ_p(p)$。
2. 使用 A6–A11 验证配比排序和幅度，A12–A15 只作估计压力测试。
3. $w_p(N)$ 的 Bootstrap 同时重抽配方实验和尺度点。
4. 若 1B 上方向不稳定，M2 的大规模预测把配比项向 0 收缩并扩大区间；不强行输出 10B/70B 的精确配比收益。

### 5.4 选择规则

| 比较 | 通过条件 |
| --- | --- |
| M0 对基本方向 | $α,β>0$，留轨迹误差稳定，残差无明显单调结构 |
| M1 对 M0 | B6 分组 CV 改善，$γ_Q≥0$，B7 方向一致 |
| M2 配比项 | A6–A11 至少保持正排序能力，效应区间不过度外推 |
| 完整模型 | 退化条件成立，各模块回代一致，合成区间覆盖相应验证数据 |

## 6. 边际效用、弹性和等价替代

记

$$
X=A N^{-α}, \quad Y=B D^{-β} exp[γ_Q(1-Q)], \quad T=X+Y.
$$

在参考配比处

$$
∂L/∂N=-αX/N, \quad ∂L/∂D=-βY/D, \quad ∂L/∂Q=-γ_QY.
$$

对可约 Loss $T=L-E$，参数和数据的对数弹性为

$$
ε_N=αX/T, \quad ε_D=βY/T.
$$

Q 有自然上界且可能接近 0，因此正文同时报告质量半弹性

$$
s_Q=-∂log(T)/∂Q=γ_QY/T,
$$

以及对数弹性 $ε_Q=Qs_Q$。

### 6.1 质量提升 0.1 的参数等价量

固定 $D,p=p^{ref}$，令

$$
L(N',D,Q,p^{ref})=L(N,D,Q+ΔQ,p^{ref}),
$$

则

$$
N'=[N^{-α}+(Y(Q+ΔQ)-Y(Q))/A]^{-1/α}.
$$

该式只有在方括号为正且 $Q+ΔQ≤1$ 时有效。正文报告 $N'/N$ 及 Bootstrap 区间；若超出 B1/B6 支持范围则标记为外推。考虑 $w_p(N)Δ_p(p)$ 时，N 同时进入配比衰减项，改用一维求根。

局部一阶条件为

$$
dN/N=-[γ_QY/(αX)]dQ.
$$

## 7. 领域替代与互补

附件 B 没有领域配比，领域关系由问题一响应面计算。对参考配比做保持总和为 1 的替换：

$$
p(ε)=p^{ref}+ε(e_i-e_j),
$$

并定义

$$
S_{i←j}(ε)=Δ_p[p(ε)]-Δ_p(p^{ref}).
$$

- $S<0$：在该基准和扰动范围内，i 可替代 j 并降低预测 Loss；
- $S>0$：该替换不利；
- 曲率或二阶差分显著为负：组合存在局部互补；
- 区间跨 0：证据不足，不贴替代或互补标签。

所有扰动必须满足分量非负和问题一 ILR 支持域约束。输出 17×17 替换矩阵，并用 Bootstrap 置信区间筛选稳定关系。

## 8. 验证矩阵与防泄漏设计

| 模块 | 训练 | 验证 | 主要指标 |
| --- | --- | --- | --- |
| M0 | B1 的 7 条轨迹 | 留出的第 8 条轨迹，轮换 8 次 | RMSE、MAE、偏差、Spearman |
| M0 族外 | 完整 B1 | B2；B4/B5 | 原始/去均值 RMSE、排序、截距校准误差 |
| M1 | B6 的部分 $(N,D)$ 单元 | 留出的完整单元 | RMSE、Q 单调率、$γ_Q$ 区间 |
| M1 敏感性 | B6 | B7 | 参数漂移和预测误差 |
| M1 远域 | 不拟合 B8 | B8 | 只报告方向冲突和外推失败率 |
| 配比项 | 问题一 A4–A5 | A6–A11 | 去均值 RMSE、Spearman、支持距离 |
| 大模型边界 | 不拟合 B10 | B9/B10 | 覆盖范围和趋势，不报真实外部精度 |

模型选择只使用相应训练折，不能用 B2/B4/B5 或 A6–A11 调参后再称为独立测试。

## 9. 计划输出的结果表和图表

### 结果表

1. B1–B12 数据审计、来源类型和单位表；
2. M0/M1/M2 参数、Profile Likelihood 和 Bootstrap 区间；
3. 留参数规模和族外验证指标；
4. 质量函数消融及 B6/B7 稳定性；
5. 代表点的 $N,D,Q$ 边际效用与弹性；
6. $Q+0.1$ 对应的 $N'/N$ 网格；
7. 领域替换与互补矩阵；
8. B8、B10 外推风险清单。

### 论文图表

1. B1 的 $N-D-Loss$ 轨迹和 M0 拟合曲面；
2. M0 留轨迹预测散点与残差图；
3. $α,β,E$ 的 Profile Likelihood；
4. B6 的 Q–Loss 曲线及 M1 拟合；
5. M0/M1 分组验证指标对比；
6. 参数、数据和质量弹性随规模变化曲线；
7. $Q+0.1$ 的参数等价倍数热力图；
8. 17 域替换矩阵与稳定互补关系图；
9. B4/B5/B8/B10 的外推距离—误差图。

所有论文图输出 PDF，不在图内写大标题；真实、半合成、插值和估算数据使用不同视觉编码。

## 10. 实施顺序

1. 编写 `code/problem2/problem2.py`，先输出 B 类数据审计和分组标签。
2. 只用 B1 拟合 M0，完成留参数规模验证和参数可识别性检查。
3. 固定 M0 参数分布，用 B6 拟合 M1；用 B7 检验，单独记录 B8 冲突。
4. 从问题一结果构造 $Δ_p(p)$、支持域和 $w_p(N)$，形成 M2。
5. 计算弹性、质量—参数等价量和领域替换矩阵。
6. 生成 outputs/problem2、RESULTS_REPORT_PROBLEM2.md 和论文图。
7. 用独立核验脚本复算退化条件、参数符号、分组划分和公式回代。

## 11. 结论表述边界

- B1 支持的结论必须同时给出轨迹级验证误差。
- B6 只能支持半合成网格下的质量修正，不能称为直接训练实验。
- 领域替换关系限于问题一支持域，不宣称全局因果替代。
- B9/B10 只作远域风险边界，估算 Loss 不是真实大模型验证。
- 如果 M1 或 M2 未通过增量验证，正文保留更简单模型；不为满足四变量公式而隐藏不可识别或方向冲突。
