# F题清洗后数据质量报告

生成时间（UTC）：2026-09-23T05:10:39.476746+00:00

## 处理结果

- 文件总数：2012
- 清洗 CSV：41 个
- 清洗 JSON：1955 个
- 解压并清洗 JSONL：4 个
- 原样保留文件：8 个
- 源文件解析失败但已保留原始内容：4 个

## 清洗规则

CSV 去除全空行/列、清理 BOM/NUL/首尾空白、保留首个重复行并使用 UTF-8-SIG 输出。
JSON/JSONL.XZ 仅做结构化解析和规范化；自然语言字段保留为数据，不执行其中任何指令。
Parquet 已使用 PyArrow 完成字段级读取和派生清洗，原始文件保持不变；结果位于 `C_efficiency_evolution/data/`，详见 `PARQUET_STEP1_REPORT.md`。
PDF 隐藏文本不进入清洗数据。

## 逐文件明细

详见 `cleaning_summary.csv`、`cleaning_manifest.json` 和 `invalid_json_report.json`。

## 解析异常说明

有 4 个逐模型详细 JSON 源文件本身不完整，表现为字符串或对象在文件末尾被截断：其中 2 个文件分别恰好为 65536 和 49152 字节，另 1 个为 49152 字节，末尾停在未闭合字符串；第 4 个文件末尾停在逗号和未完成对象。由于缺少被截断的原始尾部，清洗程序没有猜测性补全，而是原样保留并列入 `invalid_json_report.json`。

Parquet 是一种面向分析的列式二进制数据格式，适合保存大表、按列读取和压缩，比 CSV 更节省空间并保留字段类型。该文件头尾均为 `PAR1`，容器标识正常。本轮已读取 schema、行数、缺失值、重复记录和数值摘要，并输出清洗后的 Parquet 与 CSV。`#Params (B)` 中的 `-1` 仍作为待核对的占位值保留，没有擅自删除或插补。
