# F题清洗前后文件对应检查（最终版）

本报告已按最终清洗包更新。历史生成的 `attachment_size_summary.csv` 和 `source_manifest.json` 已从原始附件根目录移入注入审计目录，不再计入原始数据。

## 统计结果

| 项目 | 清洗前 | 清洗后 | 说明 |
|---|---:|---:|---|
| 文件总数 | 2010 | 2008 | 4 个损坏 JSON 移出建模包；4 个压缩文件解压；Parquet 生成清洗派生文件；另加入可见题面数据说明 |
| CSV | 40 | 41 | 原有 CSV 保持清洗后路径，另生成 1 个 Parquet 对应 CSV |
| JSON | 1958 | 1954 | 4 个无法解析的 JSON 移入隔离目录 |
| JSONL.XZ | 4 | 0 | 解压为 4 个同名 `.jsonl` |
| JSONL | 0 | 4 | 由 `.jsonl.xz` 解压得到 |
| Markdown | 7 | 8 | 原有 Markdown 保持，另加入可见层题面数据说明 |
| Parquet | 1 | 1 | 清洗后保留 `train-00000-of-00001_clean.parquet`，原始副本已从清洗目录移除 |

## 对应关系

按最终原始附件目录与清洗目录的规范化路径比较：

- 2001 个文件保持相同相对路径和文件名；
- 4 个压缩文件由 `.jsonl.xz` 变为 `.jsonl`，属于有记录的解压改名；
- 4 个 JSON 文件解析失败，已从建模数据移出，原始内容保存在 `F题_初步方案/reports/数据处理/F题_清洗审计/quarantine_invalid_json`；
- 原始 Parquet 保留在 `real_attachments`，清洗目录使用 `train-00000-of-00001_clean.parquet`，并另存一份 CSV 便于检查；
- 可见题面文本单独保存到 `F题_清洗后/题面数据说明/数据说明_可见层.md`，不包含近白色隐藏文本。

## 结论

除明确记录的压缩解压、Parquet 清洗派生、可见题面说明和 4 个隔离 JSON 外，清洗前后的数据可以按相对路径一一对应。`F题_清洗后` 不包含方案、代码或审计报告。
